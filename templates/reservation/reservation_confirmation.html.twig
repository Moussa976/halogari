{% extends 'base.html.twig' %}

{% block title %}Confirmation de réservation{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="text-center mb-4">
        <h2>🎉 Réservation enregistrée</h2>
        <p>
            Merci {{ app.user.prenom }}, votre demande de réservation a bien été enregistrée.<br>
            <strong>Elle est maintenant en attente de validation par le conducteur.</strong>
        </p>
        <p class="text-muted mt-2">
            Vous recevrez une notification dès que le conducteur aura confirmé ou refusé votre réservation.
        </p>
    </div>

    <div class="card">
        <div class="card-body">
            <h5 class="card-title">Détails du trajet réservé</h5>
            <p><strong>Départ :</strong> {{ reservation.trajet.depart }}</p>
            <p><strong>Arrivée :</strong> {{ reservation.trajet.arrivee }}</p>
            <p><strong>Date :</strong> {{ reservation.trajet.dateTrajet|date('d/m/Y') }}</p>
            <p><strong>Heure :</strong> {{ reservation.trajet.heureTrajet|date('H:i') }}</p>
            <p><strong>Places réservées :</strong> {{ reservation.places }}</p>
            <p><strong>Montant total :</strong> {{ reservation.prixTotal }} €</p>
        </div>
    </div>

    <div class="mt-4 text-center">
        <a href="{{ path('app_trajets_reservations') }}" class="btn btn-primary">Voir mes réservations</a>
    </div>
</div>
{% endblock %}
